package server

class SmartController {
    //could be TEMP / PUMP / STIR
    String type     
    String identifier
    String language
    String siwtchCommand
    String readSensorCommand
    String threshold
    static constraints = {
    }
}
